verify_id: stock_analyzer/2026-07-07-governor-artifact-persistence
verify_after: 2026-07-07T12:00:00+09:00
project: stock_analyzer
description: |
  2026-07-04 監査の最重要修正 — strategy_governor 成果物の CI 永続化 —
  の効果確認。

  問題: stock-analysis.yml / weekly-review.yml の commit ステップが
  data 3 ファイルしか git add しておらず、governor の
  strategy_change_log.json / strategy_change_proposals.json /
  rollback snapshot / verify YAML が runner ごと毎回消えていた。
  結果: one-batch-at-a-time gate は常に素通り (7/1 に 9 時間差で
  2 batch 適用)、weight 変更の効果検証は一度も実行されず、
  auto-rollback も snapshot 不在で不可能だった。

  修正: 両 workflow の git add に governor 成果物を追加 +
  prepare / apply-review ステップに GH_TOKEN を渡して verify issue
  自動作成を有効化 (permissions に issues: write も追加)。

  確認内容 (手動 / Stage 2 Claude 向け手順):
  1. `git log --oneline --since=2026-07-06 -- stock_analyzer/data/strategy_change_proposals.json`
     に月曜 7/6 以降の cron commit が含まれること (bayesian proposal は
     毎 cron 提出されるので、月曜朝 cron 後には必ず commit されるはず)
  2. 次に calibration_zone が green になって weight 変更が適用された
     とき: verify/stock_analyzer/wts-*.yml + snapshots/wts-*.json が
     repo に commit され、pending-verify label の issue が自動作成
     されること (これは zone 次第で時期未定のため、1. だけで
     まず合格判定してよい)
kind: manual
args: {}
max_attempts: 5
retry_after_hours: 48
relates_to: 2026-07-04 audit fixes
success_message: |
  ✅ stock_analyzer: governor 成果物が CI から repo に永続化されている
  ことを確認。one-batch-at-a-time gate と auto-rollback が state を
  持てるようになった。次の green zone 移行時に wts-*.yml + issue の
  自動生成まで確認できれば検証ループは完全に閉じる。
