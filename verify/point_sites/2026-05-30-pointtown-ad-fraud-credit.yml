verify_id: point_sites/2026-05-30-pointtown-ad-fraud-credit
verify_after: 2026-05-30T22:00:00+09:00
project: point_sites
relates_to: "5c9f753 (pointtown: 6 ad-fraud visit-only daily_wizards)"
description: |
  2026-05-23 commit 5c9f753 で pointtown adapter に 6 個 ad-fraud visit-only
  wizard を追加 (gacha / easygame / gesoten / brain_quiz / nazotore / pointq)。

  ``recent_run_log_grep`` で 1 週間後の pointtown scheduled run log に新規
  wizard ログが継続して出ていることを確認。balance 動向は user が Slack
  channel (SLACK_CHANNEL_POINTTOWN) で観察。
kind: recent_run_log_grep
args:
  workflows:
    - pointtown.yml
  pattern: "pointtown_(gacha|easygame|gesoten|brain_quiz|nazotore|pointq) wizard"
  lookback: 5
  require_all: true
max_attempts: 7
retry_after_hours: 24
success_message: |
  ✅ pointtown ad-fraud visit-only wizards 1 週間継続稼働。
  実 yield は SLACK_CHANNEL_POINTTOWN の balance delta で確認。
